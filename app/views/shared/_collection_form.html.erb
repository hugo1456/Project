<div class="modal fade" id="new-collection-form" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
        <h4 class="modal-title"> Create new collection</h4>
      </div>
      <div class="modal-body">
         <div class="row">
           <div class="col-md-12">
          <%= form_for(:collection, url: user_collections_path(current_user), html: { class: "form-horizontal collection-form" }) do |f| %>

          <div class= "form-group"><%= f.text_field :name, placeholder: "Name your collection, max 50", maxlength: "50", class: "form-control", autofocus: true %></div>

          <div class= "form-group"><%= f.text_area :description, placeholder: "About what is your collection?, max 500", rows: "3", maxlength: "500", cols: "60", wrap: "hard", class: "form-control" %></div>
          </div>
        </div>
      </div>
      <div class="modal-footer">
        <div class= "form-group"><%= f.submit "Create", class: "btn btn-default" %></div>  
        <% end %>
      </div>
    </div>
  </div>
</div>
